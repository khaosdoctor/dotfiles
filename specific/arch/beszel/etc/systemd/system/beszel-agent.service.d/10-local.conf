[Service]
# Docker socket is root:docker 0660, so the beszel user needs the group to
# report container metrics. video/render are belt-and-braces for rocm-smi;
# /dev/dri/renderD128 is already world-accessible on this box.
SupplementaryGroups=docker video render
