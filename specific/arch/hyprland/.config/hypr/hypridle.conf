general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid starting multiple hyprlock instances.
    on_lock_cmd = systemctl --user stop --now waybar
    on_unlock_cmd = systemctl --user restart --now waybar
    before_sleep_cmd = loginctl lock-session; systemctl --user stop --now waybar     # lock before suspend
    after_sleep_cmd = hyprctl dispatch dpms on; systemctl --user restart --now waybar  # dpms on so display wakes without a double keypress
}

# Lock first, then DPMS off shortly after.
# This ensures only hyprlock deals with the DPMS resume,
# preventing other apps from losing frame callbacks.
listener {
    timeout = 1800
    on-timeout = loginctl lock-session            # lock screen first
}

listener {
    timeout = 1820
    on-timeout = hyprctl dispatch dpms off         # turn monitors off after lock is up
    on-resume = hyprctl dispatch dpms on          # dpms on already restores frame callbacks; forcerendererreload wedged the 7900 XTX DCN engine
}

listener {
    timeout = 7200
    on-timeout = systemctl suspend                # suspend pc
}
