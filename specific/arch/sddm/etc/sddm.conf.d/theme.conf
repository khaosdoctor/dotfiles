[Theme]
# Pinned 2026-08-13. The sddm-randomizer.service used to rewrite this line on every
# boot; it is now disabled, so this value is stable. Re-enable with:
#   sudo ln -s <dotfiles>/specific/arch/sddm/etc/systemd/system/sddm-randomizer.service \
#     /etc/systemd/system/sddm-randomizer.service && sudo systemctl enable sddm-randomizer
Current=where_is_my_sddm_theme

[X11]
DisplayCommand=/etc/sddm.conf.d/XSetup
